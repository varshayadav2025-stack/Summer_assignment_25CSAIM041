#include <stdio.h>
int main()
{
    int n, count = 0;
    printf("enter the decimal number: ");
    scanf("%d", &n);
    while (n > 0)
    {
        n = n / 10;
        count++;
    }
    printf("the number of digits in the decimal number is: %d", count);
    return 0;
}